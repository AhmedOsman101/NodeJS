function product(num, multi) {
    return num * multi;
}

function divide(num, divider) {
    return num / divider;
}

function sum(num, sum) {
    return num + sum;
}

function minus(num, minus) {
    return num - minus;
}

module.exports = { minus, divide, product, sum };
